import { Component } from 'react';
import './index.less';

export default class index extends Component {
  render() {
    return (
      <div>
        index
        {/* <canvas id="webgl" width="500" height="500" style={{background: '#000'}} className='canvas' ></canvas> */}
      </div>
    );
  }
}
